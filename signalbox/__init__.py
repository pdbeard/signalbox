"""Signalbox - shell task execution, logging, alerting, and scheduling exports.

The CLI lives in signalbox.cli; core functionality is in the sibling modules
(executor, config, validator, log_manager, alerts, runtime, exporters).
"""
